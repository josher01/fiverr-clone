<div class="cat-bar">
  <div class="container-fluid">
    <ul class="nav nav-fill">
      <% @categories.each do |category| %>
        <li class="nav-item">
          <%= link_to category.title, category_services_path(category), class: 'nav-link' %>
        </li>
      <% end %>
    </ul>  
  </div> 
</div>